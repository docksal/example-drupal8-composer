- name: fin ps
  shell: "ls -al"
  args:
    chdir: "{{ docroot_path }}/sites/default"
  register: debug

- name: debug
  debug:
    msg: "{{debug}}"

- name: Init andock environment
  command: "fin init-andock"
  args:
    chdir: "{{ docroot_path }}"

